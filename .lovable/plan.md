# הסבר ופעולה: עופר ולקוחות SEO

## מצב נוכחי (בדקתי בקוד וב-DB)

הלוגיקה ל"SEO רואה את כל לקוחות SEO" **כבר מיושמת במלואה** — גם בצד הקליינט וגם ב-RLS:

- **`src/pages/Clients.tsx` שורה 73**: `isSeoOnlyViewer = isSeo && !isTeamManager && !isOwner && !isSuperAdmin` — תפקיד `seo` גובר על `campaigner`.
- **שורות 419–424**: כש-`isSeoOnlyViewer` true → הסינון מציג כל לקוח עם `is_seo_client=true` או `services` שמכיל `'seo'`, ללא שיוך אישי.
- **RLS פוליסי `SEO users view SEO-tagged clients`**: מאפשר SELECT לכל משתמש עם תפקיד `seo` על לקוחות מתויגי SEO ב-tenant.

## מה שמצאתי על עופר

- עופר (`oferhaik@gmail.com`) → roles = **{seo, campaigner}** ✅
- ב-tenant שלו יש **8 לקוחות** עם `is_seo_client=true` ו-1 נוסף עם `'seo'` ב-`services` → סה"כ ~8–9 לקוחות SEO שאמורים להופיע אצלו.

המשמעות: ברגע שהגרסה הזו תהיה ב-Production הוא יראה אותם. הבעיה היחידה היא שהשינויים האחרונים עדיין לא פורסמו.

## תוכנית

1. **פרסום (Publish)** — אין שינוי קוד נדרש. צריך ללחוץ Publish כדי שעופר יקבל את הגרסה עם לוגיקת ה-SEO.
2. **אחרי פרסום** — לבקש מעופר לעשות רענון קשה (Ctrl/Cmd+Shift+R) או יציאה והתחברות מחדש.
3. **אם עדיין לא רואה** — לבדוק שעופר נכנס ל-tenant הנכון (`/t/marketingcaptain/clients`) ושהלקוחות שהוא מצפה לראות אכן מתויגים `is_seo_client=true` או יש להם `'seo'` ב-services. אם לא — לסמן אותם.

## ללא שינויי קוד בשלב הזה
לא צריך לגעת בקוד. אם אחרי פרסום ורענון עדיין יש בעיה, נחקור לעומק (למשל אם רוצים שגם קמפיינר ללא תפקיד `seo` יראה לקוחות SEO — זה שינוי נפרד).
