מצאתי שהוצאות ה-SEO קיימות בבסיס הנתונים: 26 לקוחות, ₪850 לכל לקוח, בחודשים ינואר-אפריל 2026. הבעיה היא בצד התצוגה של הנהלת החשבונות: המסך מסתמך על רשומות `finance` לפי חודש/טנאנט, ובמצב של לקוחות משויכים/משותפים זה לא תמיד משתקף בטבלה ובחלון עריכת הלקוח.

התוכנית לתיקון:

1. להפוך הוצאות SEO לחישוב קבוע לפי הלקוח
   - כל לקוח שמסומן כ-SEO (`is_seo_client`) או שיש לו שירות `seo` יקבל אוטומטית הוצאה חודשית של ₪850.
   - ההוצאה תחושב לכל חודש שנבחר — ינואר, פברואר, מרץ, אפריל וגם חודשים קדימה.
   - אם כבר קיימת רשומת `finance` של SEO לאותו לקוח ולאותו חודש, לא נספור כפול.

2. לתקן את טבלת הנהלת החשבונות
   - בעמוד `AccountingIntegrations`, עמודת “הוצאות קבועות” תציג גם את ה-SEO האוטומטי.
   - בפירוט הקטן מתחת לסכום יוצג למשל: `SEO: ₪850`.
   - הסיכום העליון של “הוצאות” ו-“רווח” יתעדכן לפי אותו חישוב.

3. לתקן את חלון עריכת הלקוח
   - בתוך עריכת לקוח יוצג סעיף “הוצאות החודש” גם אם אין רשומת `finance` ידנית.
   - לקוח SEO יציג שם את הוצאת ה-SEO החודשית ₪850.

4. לשמור התאמה בין נתונים קיימים לבין תצוגה
   - לא נמחק את רשומות ה-SEO שכבר הוכנסו ל-`finance`.
   - הקוד ימנע כפילות: אם קיימת רשומת SEO בחודש מסוים, היא תוצג כרשומת finance; אם לא קיימת, ההוצאה תחושב אוטומטית לפי שיוך הלקוח ל-SEO.

קבצים שאעדכן לאחר אישור:
- `src/pages/AccountingIntegrations.tsx`
- `src/components/forms/EditClientDialog.tsx`