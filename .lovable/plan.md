## הבעיה

יצרתי 26 הוצאות SEO ושמרתי אותן עם `tenant_id` של הלקוח (DMM), בעוד בפועל הן צריכות להיות תחת הטננט שמבצע את הפעולה (Marketing Captain). בנוסף, עמוד "הנהלת חשבונות / כספים" (Finance) מסנן רק לפי `tenant_id` של הטננט הנוכחי בלבד, ולכן גם אם נתקן את ההוצאות — ההזנות הבאות לא יוצגו אם הקוד ימשיך להציב את `tenant_id` של הלקוח.

## מה אעשה

### 1. תיקון נתונים קיים (SQL)
עדכון 26 הוצאות ה-SEO שיצרתי (קטגוריה `S