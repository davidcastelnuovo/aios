מצאתי את הבעיה: א.י זוהר עץ כן מתויג SEO (`services = ['seo']`), ועופר כן מחזיק גם `seo` וגם `campaigner`. הבעיה היא בצד המסך: בגלל שיש לו גם role של `campaigner`, הקוד מסווג אותו כ־campaigner מוגבל ומסנן רק לקוחות שמשויכים אליו ב־`client_team`. לעופר אין כרגע שיוכים שם, ולכן הוא לא רואה כלום — למרות ש־RLS כבר מאפשר לו לראות לקוחות SEO.

תוכנית התיקון:

1. לעדכן את `src/pages/Clients.tsx` כך ש־SEO יקבל עדיפות על סינון campaigner:
   - אם משתמש הוא `seo`, הוא יראה את כל הלקוחות שמוגדרים SEO.
   - רק campaigner שאינו SEO יוגבל ללקוחות שמשויכים אליו.

2. לעדכן את תנאי טעינת הנתונים וה־query key באותו מסך כדי שהשאילתה לא תחכה ל־`campaignerId` כאשר למשתמש יש role של SEO.

3. להשאיר את ה־RLS הקיים כפי שהוא, כי הבדיקה הראתה שהוא כבר מזהה נכון:
   - `services @> ['seo']`
   - או `is_seo_client = true`
   - כולל סוכנויות משותפות דרך `agency_tenant_access`.

4. לוודא אחרי השינוי שהלקוח “א.י זוהר עץ” והלקוחות SEO של DMM-MC עוברים את סינון המסך עבור עופר.