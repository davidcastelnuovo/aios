## Change

Describe the feature/fix and the affected modules.

## Environment and safety

- [ ] Work is on a `feature/*`, `fix/*`, or `refactor/*` branch.
- [ ] Preview uses non-Production Supabase/integration credentials.
- [ ] Database changes are versioned under `supabase/migrations/` and tested outside Production.
- [ ] External side effects are blocked, redirected, mocked, allowlisted, or dry-run outside Production.
- [ ] No direct Production database, Edge Function, secret, webhook, or customer-data change was made.

## Verification

- Vercel Preview URL:
- In-app route:
- Checks performed:
- Known limitations:

## Promotion

- [ ] Validated in Preview.
- [ ] Validated in persistent Staging.
- [ ] Production approval received explicitly (`מאשר לפרודקשן`).
