---
description: AIOS has a development environment. Never claim it does not exist.
alwaysApply: true
---

# Development environment EXISTS

There **is** a development environment. Never tell David or Carmen that it does not exist.

- **Development environment** = Vercel Preview URL of the current branch (Staging data). **Persistent Staging** = `https://after-lead-git-develop-aios-crm.vercel.app` (`develop`); verify the custom domain `https://staging.aios.co.il` before presenting it as available.
- **Staging** = git branch `develop` → persistent Staging Supabase + Vercel `develop` deploy.
- **Production** = `main` only. Never use Production as a development environment.
- Normal flow: merge to `develop` → verify on Staging → `מאשר לפרודקשן` → merge `develop` → `main`.
- This Cloud Agent's local `.env` still points at Production. That is **not** proof that Staging is missing.
- Always send David the Vercel Preview URL when work finishes.
- Merge to `main` only after he has that link and says `מאשר לפרודקשן`.
- Source of truth: `docs/ENVIRONMENTS.md`.

If asked whether a staging / development environment exists: **yes**. Send the Preview URL.

Before describing Staging data as current or outbound delivery as blocked, verify deployment/containment results and mirror freshness as specified in `docs/ENVIRONMENTS.md`. Connected-looking WhatsApp integrations do not authorize delivery; keep Production WhatsApp tokens out of Staging.

**Never mix WhatsApp connections:** Manus (`manus_wa`, Carmen) ≠ Green API (`green_api`, operator). Carmen group lists must come from Manus only — never the full `whatsapp_groups` table. See `.cursor/rules/whatsapp-connections.mdc`.
