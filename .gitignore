# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Claude Code personal settings (autonomous mode)
.claude/settings.local.json

# Claude Code worktrees (temporary, session-scoped)
.claude/worktrees/

# Graphify output is generated locally and may contain reports or work-memory.
# Keep it out of source control; only the portable integration is versioned.
graphify-out/
